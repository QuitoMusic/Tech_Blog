<!DOCTYPE html>
<html>
<head>
  <title>{{post.title}} - Tech Blog</title>
</head>
<body>
  <h1>{{post.title}}</h1>
  <p>{{post.content}}</p>
  <p>Posted by {{post.User.username}} on {{formatDate post.createdAt}}</p>
  <h2>Comments</h2>
  <ul>
    {{#each comments}}
      <li>
        <p>{{this.content}}</p>
        <p>Commented by {{this.User.username}} on {{formatDate this.createdAt}}</p>
      </li>
    {{/each}}
  </ul>
  <h2>Add a Comment</h2>
  <form action="/comment/{{post.id}}" method="POST">
    <textarea name="content" placeholder="Your comment" required></textarea>
    <button type="submit">Submit Comment</button>
  </form>
</body>
</html>
